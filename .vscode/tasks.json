{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "g++ build",
      "command": "g++",
      "args": [
        "-fdiagnostics-color=always",
        "-g",
        "-O0",
        "-Wall",
        "-Wextra",
        "-std=c++23",
        "${file}",
        "-o",
        "a.out"
      ],
      "options": {
        "cwd": "${fileDirname}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": "$gcc",
      "detail": "Build C++ file with g++ from the Nix devShell"
    }
  ]
}
